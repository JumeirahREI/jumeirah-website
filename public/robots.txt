# Allow all crawlers
User-agent: *
Allow: /

# NOTE: The blanket Allow: / above intentionally permits AI and LLM crawlers
# (including GPTBot, ClaudeBot, PerplexityBot, CCBot, Google-Extended, Applebot-Extended).
# This is a deliberate decision to allow AI training and answer-engine indexing,
# not an accidental default or oversight.

# Disallow admin or private paths (if any)
# Disallow: /admin/
# Disallow: /api/

# Sitemap location
# NOTE: The URL below is hardcoded as a static file with no build-time env var access.
# If the production domain ever changes from https://jumeirahye.com/, this line must
# be updated manually — it will not automatically pick up env var changes like the rest
# of the codebase does.
Sitemap: https://jumeirahye.com/sitemap.xml
